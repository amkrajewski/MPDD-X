name: FeaturizationWorker

on: 
  issue_comment:
    types: [created]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    steps:
    - uses: actions/checkout@v4

    - name: Respond to comment
      uses: actions/github-script@v5
      with:
        script: |
          const issueComment = context.payload.comment;
          const issue = context.issue;
          const regex = /(https:\/\/[^\s]+\.zip)/g;
          const matches = issueComment.body.match(regex);

          let message;
          if (matches && matches.length > 0) {
            message = '👍 [1/3] I found a ZIP file linked in your comment. I will now attempt to download it and validate contents.';
          } else {
            message = '😢 Your comment does not contain any direct links to ZIP files. Please note that certain cloud providers do not support that. Try to directly drag-and-drop into a comment.';
          }

          github.rest.issues.createComment({
            owner: issue.owner,
            repo: issue.repo,
            issue_number: issue.number,
            body: message
          });

          return matches ? matches[0] : null;
          
    - name: Download attachment
      id: download
      run: |
        if [ -n "${{ steps.respond.outputs.result }}" ]; then
          curl -O ${{ steps.respond.outputs.result }} || echo "::set-output name=downloaded::false"
        fi
    - name: Send message if download failed
      uses: actions/github-script@v5
      if: steps.download.outputs.downloaded == 'false'
      with:
        script: |
          const issue = context.issue;
          const message = '😢 Could not download the linked ZIP file.';

          github.rest.issues.createComment({
            owner: issue.owner,
            repo: issue.repo,
            issue_number: issue.number,
            body: message
          });

    - name: Unzip attachment into structures
      uses: montudor/action-zip@v1
      with:
        args: unzip -qq attachment.zip -d structures

    - name: Verify file extensions
      uses: actions/github-script@v5
      with:
        script: |
          const fs = require('fs');
          const path = require('path');

          const issueComment = context.payload.comment;
          const issueUrl = issueComment.issue_url;

          const fileNames = fs.readdirSync('structures');
          for (const fileName of fileNames) {
            const extension = path.extname(fileName);
            if (extension !== '.poscar' && extension !== '.cif') {
              await github.issues.createComment({
                issue_url: issueUrl,
                body: '😢 Not all files have a POSCAR or CIF extension.'
              });
              return;
            }
          }

          await github.issues.createComment({
            issue_url: issueUrl,
            body: '🎉 Received your strcuture files:\n' + fileNames.join('\n')
          });

    - name: Set up Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"
        cache: 'pip'
        cache-dependency-path: 'featurization/requirements.txt'

    - name: Install dependencies
      run: |
        python -m pip install -r featurization/requirements.txt

    - name: Test that pysipfenn is available
      run: python -c "import pysipfenn; c = pysipfenn.Calculator()"
